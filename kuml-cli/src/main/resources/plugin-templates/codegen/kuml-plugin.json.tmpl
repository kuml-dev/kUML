{
  "schemaVersion": 1,
  "id": "{{pluginId}}",
  "name": "{{pluginName}}",
  "version": "1.0.0",
  "kumlVersionRange": "{{kumlVersionRange}}",
  "extensions": [
    {
      "category": "codegen",
      "implementation": "{{packageName}}.{{className}}",
      "id": "{{artifactId}}-codegen"
    }
  ],
  "permissions": ["fs.write"],
  "maintainer": "{{maintainer}}",
  "homepage": "{{homepage}}",
  "licenseSpdx": "{{licenseSpdx}}"
}
