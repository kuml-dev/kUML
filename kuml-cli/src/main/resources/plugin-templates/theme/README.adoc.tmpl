= {{pluginName}}
:toc:
:icons: font

A kUML *theme* plugin.

== Build

[source,bash]
----
./gradlew build
----

The plugin JAR is produced at `build/libs/{{artifactId}}-1.0.0.jar`.

== Install

[source,bash]
----
kuml plugin install build/libs/{{artifactId}}-1.0.0.jar
----

== Plugin metadata

[cols="1,3"]
|===
| Plugin id    | `{{pluginId}}`
| Category     | theme
| kUML range   | `{{kumlVersionRange}}`
| Maintainer   | {{maintainer}}
| License      | {{licenseSpdx}}
|===

== Customize

Edit `src/main/kotlin/{{packageDir}}/{{className}}.kt` — look for `TODO` markers.

== License

Licensed under {{licenseSpdx}}.
