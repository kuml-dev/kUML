{
  "schemaVersion": 1,
  "id": "{{pluginId}}",
  "name": "{{pluginName}}",
  "version": "1.0.0",
  "kumlVersionRange": "{{kumlVersionRange}}",
  "extensions": [
    {
      "category": "reverse",
      "implementation": "{{packageName}}.{{className}}",
      "id": "{{artifactId}}-reverse"
    }
  ],
  "permissions": ["fs.read"],
  "maintainer": "{{maintainer}}",
  "homepage": "{{homepage}}",
  "licenseSpdx": "{{licenseSpdx}}"
}
