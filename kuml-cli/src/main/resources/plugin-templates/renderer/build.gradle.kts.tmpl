plugins {
    kotlin("jvm") version "2.4.0"
    id("org.jlleitschuh.gradle.ktlint") version "14.2.0"
}

group = "{{groupId}}"
version = "1.0.0"

repositories {
    mavenCentral()
}

kotlin {
    jvmToolchain(21)
    explicitApi()
}

dependencies {
    implementation("dev.kuml:kuml-plugin-api-renderer:{{kumlVersion}}")
    implementation("dev.kuml:kuml-plugin-api-core:{{kumlVersion}}")
    implementation("dev.kuml:kuml-core-model:{{kumlVersion}}")
    testImplementation("io.kotest:kotest-runner-junit5:6.1.11")
    testImplementation("io.kotest:kotest-assertions-core:6.1.11")
}

tasks.withType<Test>().configureEach { useJUnitPlatform() }
