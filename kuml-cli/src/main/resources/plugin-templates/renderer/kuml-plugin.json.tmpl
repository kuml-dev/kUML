{
  "schemaVersion": 1,
  "id": "{{pluginId}}",
  "name": "{{pluginName}}",
  "version": "1.0.0",
  "kumlVersionRange": "{{kumlVersionRange}}",
  "extensions": [
    {
      "category": "renderer",
      "implementation": "{{packageName}}.{{className}}",
      "id": "{{artifactId}}-renderer"
    }
  ],
  "permissions": [],
  "maintainer": "{{maintainer}}",
  "homepage": "{{homepage}}",
  "licenseSpdx": "{{licenseSpdx}}"
}
