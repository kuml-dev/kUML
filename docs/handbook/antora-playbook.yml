#
# Antora playbook — used for both local preview and GitHub Pages deployment.
#
# Local preview:
#   npx antora generate docs/handbook/antora-playbook.yml
#   open docs/handbook/build/site/index.html
#
# CI deployment: .github/workflows/docs.yml builds this and pushes to gh-pages.
# The live site is served at https://docs.kuml.dev
#
site:
  title: kUML Handbook
  url: https://docs.kuml.dev
  start_page: kuml::index.adoc

content:
  sources:
    - url: ../..
      branches: HEAD
      start_path: docs/handbook

ui:
  bundle:
    url: https://gitlab.com/antora/antora-ui-default/-/jobs/artifacts/HEAD/raw/build/ui-bundle.zip?job=bundle-stable
    snapshot: true

output:
  dir: ./build/site

asciidoc:
  extensions:
    # When the kuml-asciidoc Maven artefact is on the runtime classpath, this
    # picks up the `[kuml]` block macro and renders embedded diagrams inline.
    # If the extension is unavailable, listings render as plain Kotlin script
    # source — which still reads correctly.
    - '@asciidoctor/kuml'
  attributes:
    page-toclevels: 3
    source-highlighter: highlight.js
